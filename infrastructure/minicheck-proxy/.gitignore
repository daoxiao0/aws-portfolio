# State can contain resource attributes and must never reach a public repository.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
tfplan
*.tfplan

# archive_fileが terraform plan/apply のたびに再生成するビルド成果物
lambda-proxy.zip
